services:
  bloom:
    build: .
    container_name: bloom
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - bloom_data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/period_tracker.db
      - PORT=8080

volumes:
  bloom_data:
